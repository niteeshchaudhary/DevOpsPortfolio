# fetch or create svg icons for all popular devops tools like:

1) github, git, github actions
2) jenkins, gitlab
3) argocd
4) docker, kubernets
5) aws,gcp,azure
6) lambda,s3,kinesis,rds,kinesis,sqs,dynamodb,firebase,firestore,dms,cloudfront,cloudformation, amplify, waf
7) cloudflare
8) firewall
9) istio
10) terraform, ansible
11) elastic, kibana, logstsh,
12) grafana, prometheus
13) redis,kafka, rabbitmq

